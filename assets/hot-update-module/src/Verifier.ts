/** 
 * 校验器
 * 负责校验下载文件（MD5/大小）和文件签名。
 */
export default class Verifier {
    constructor() { }


    /** 校验下载文件（MD5/大小） */
    verifyAssets(files: any) {
        // 校验下载文件（MD5/大小）
    }


    /** 校验文件签名 */
    verifySignature(file: any) {
        // 校验文件签名
    }

    
}